# Architecture

OpenShapeForge is an extensible, compiler-driven core ERP platform. The single
source of truth is a set of YAML authoring files. A compiler turns them into
every runtime artifact the platform needs — the Postgres schema (with
row-level security), Kysely types, a runtime manifest, a Keycloak realm, and
plugin outputs — and the runtimes are generic engines driven by those
artifacts. Adding an entity is a YAML change, not a code change.

## The pipeline

```
authoring.config.yaml            layer + plugin registration (repo root)
        │
        ▼
Authoring layers (Kustomize-style base + overlays + plugin layers)
  packages/compiler/config/authoring/     base layer in this repo
  <plugin>/authoring/                     appended automatically per plugin
        │  resolved & merged (multi-layer -> .authoring-build/, gitignored)
        ▼
Compiler  (packages/compiler, `bun run generate`)
  platform-schema.yaml  ──┐  base platform tables (schema_migrations, entity_events)
  plugin platform tables ─┤  merged in (collisions are errors)
  entity YAML contracts  ─┘  compiled once, promoted to tables (erp.*)
        │
        ▼
Generated artifacts (all gitignored; reproducible)
  apps/api/src/generated/db/schema.sql       DDL + RLS policies + FKs (idempotent)
  apps/api/src/generated/db/types.ts         Kysely table types
  apps/api/src/generated/db/manifest.json    runtime manifest (+ checksum)
  apps/api/src/generated/rest/openapi.json   OpenAPI 3.1 for opted-in entities
  apps/api/src/generated/mcp/tools.json      MCP tool catalog (field schemas)
  packages/compiler/config/referentiedata/core-by-groep.json
  keycloak/openshapeforge-realm.json         realm import for the compose stack
  apps/api/src/generated/workflow/*          workflow plugin (api side)
  docs/entities.generated.md                 entity-docs plugin
        │
        ▼
Runtimes and tools (all manifest-driven, no per-entity code)
  apps/api          generic GraphQL CRUD engine + entity-event journal
  db:migrate        roll-forward migrator keyed on the manifest checksum
  test:e2e          e2e suite derived from the manifest at load time
  test:perf         k6 scenarios/thresholds derived from the manifest
  docker compose    Keycloak imports the generated realm
```

Key entry points:

- `packages/compiler/src/index.ts` — `collectAllArtifacts(repoRoot)` computes
  every artifact in memory (the one code path shared by `generate` and the
  check scripts); `runCompiler({ repoRoot })` writes them to disk.
- `packages/compiler/src/active-manifest.ts` — resolves layers, loads plugins,
  merges plugin platform tables into `config/platform-schema.yaml`, promotes
  compiled entity contracts into the manifest. Memoized per repo root so a
  double-run sees identical inputs.
- `apps/api/src/graphql/generated-entity-schema.ts` — builds the whole GraphQL
  schema from `manifest.json` at module load. See [api.md](api.md).

## Generated vs hand-written

Everything the compiler writes is **generated**: it lives under a declared
generated root or carries a `generated` name, starts with a
"Generated by OpenShapeForge Service Compiler. Do not edit" header where the
format allows one, and is **gitignored** (see `.gitignore`: the
`apps/api/src/generated/` roots, `keycloak/*-realm.json`, `.authoring-build/`,
`docs/entities.generated.md`). You never edit generated files; you edit the
authoring YAML (or the compiler/plugin) and rerun `bun run generate`.

The ownership boundary is explicit code, not convention alone:

- `packages/compiler/src/generated-artifact-paths.ts` lists compiler-owned
  roots and single files (including the dormant `apps/web/**` roots).
- Each plugin declares its own outputs via `ownedPaths` (see
  [plugins.md](plugins.md)).
- `scripts/check-generated-artifacts.mjs` walks all owned roots/files and
  fails on **orphans** (files inside an owned root that a fresh generation
  would not produce) and on **stale** files (disk differs from a fresh
  in-memory generation).

Hand-written code is everything else: the compiler itself, the generic API
engine (`apps/api/src/**` outside `src/generated/`), migrations, scripts and
tests. The generic engine deliberately contains zero per-entity code — new
entities are covered by the API, the e2e suite, and the load tests without
touching any of them.

## Determinism gates

The compiler must be a pure function of the repo state. That is enforced, not
assumed:

- `bun run check:generated` runs the entire pipeline **twice** (plugins
  included), hashes each artifact group (db, referentiedata, UI, Keycloak,
  each plugin separately) and fails on any difference. It then performs the
  stale check, a temp-write proof (every artifact lands as exactly one file),
  a web-shard coverage check (only when `apps/web` exists, guarded by
  `expectedGeneratedCrudEntityCount`), and the orphan scan.
- `bun run check:authoring-local` is the lighter authoring-side gate: double
  generation hash, all JSON schemas under
  `packages/compiler/config/schemas/` parse, and the active manifest has
  tables.

Because the gates hash byte-for-byte, anything nondeterministic — map
iteration order, timestamps, `Math.random`, unsorted directory walks — fails
CI immediately. Plugins are held to the same contract.

## Dormant surfaces (present, intentionally inactive)

- **Web UI generation** exists in the compiler
  (`generateAuthoringUiArtifacts`, the EJS templates under
  `src/authoring/generators/templates/`) but activates only when an
  `apps/web` directory exists in the repo. Without one, `generate` emits the
  data layer, referentiedata snapshot, Keycloak realm, and plugin artifacts
  only. See [consuming.md](consuming.md#re-enabling-web-generation).
- **Entity events have no consumers yet.** Mutations append journal rows in
  `platform.entity_events`; no outbox/realtime fanout ships in this repo, and
  no GraphQL query exposes the journal. See
  [api.md](api.md#the-entity-event-journal).
- **Entity hooks** (`hooks:` in entity YAML — before/after
  create/update/delete) are parsed and carried on the compiled contract, but
  the API runtime never executes them.
- **Contexts and mappings** (`contexts/`, `mappings/` authoring directories)
  are supported by the loader but unused in this repo. See
  [authoring.md](authoring.md#contexts-and-mappings-supported-unused-here).
- **`rowScope` multi-axis RLS** (group/user/bypass axes) exists in the schema
  emitter and the `app.*` helper functions, but no current table declares it;
  every tenant-scoped table today gets the plain
  `tenant_id = app.current_tenant()` policy.

## Further reading

| Topic | Doc |
| --- | --- |
| Entity YAML anatomy, catalogs, adding an entity | [authoring.md](authoring.md) |
| Layers, overlays, `entityPatch` merge semantics | [layers.md](layers.md) |
| Compiler plugin contract + shipped examples | [plugins.md](plugins.md) |
| GraphQL engine, auth, RLS, events, local stack | [api.md](api.md) |
| Proof gates, e2e suite, HTML reports, k6 | [testing.md](testing.md) |
| Roll-forward + versioned migrations, drift | [migrations.md](migrations.md) |
| Using the compiler from a host repo | [consuming.md](consuming.md) |
