schemaVersion: 1
kind: semanticTypeCatalog

# Core semantic types — always available
types:
  email:
    label: { en: Email, nl: E-mail }
    valueType: string
    icon: AtSign
    validation:
      pattern: "^[^@]+@[^@]+\\.[^@]+$"
      maxLength: 254
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: pii
      category: contactgegevens
    audit: true

  phone:
    label: { en: Phone number, nl: Telefoonnummer }
    valueType: string
    icon: Phone
    validation:
      pattern: "^\\+?[0-9\\s\\-()]{7,20}$"
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: pii
      category: contactgegevens

  iban:
    label: { en: IBAN, nl: IBAN }
    valueType: string
    icon: CreditCard
    validation:
      pattern: "^[A-Z]{2}[0-9]{2}[A-Z0-9]{4,30}$"
      minLength: 15
      maxLength: 34
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: confidential
      category: financieel
    retention:
      mode: inline
      scope: field
      duration: P7Y
      disposition:
        action: anonymize
      reason: { en: "Financial data must be retained for 7 years (Dutch tax law)", nl: "Financiële gegevens moeten 7 jaar bewaard worden (fiscale bewaarplicht)" }
    audit: true

  url:
    label: { en: URL, nl: URL }
    valueType: string
    icon: Link
    validation:
      pattern: "^https?://"
      maxLength: 2000
    render:
      display: TextDisplay
      input: Input

  webUrl:
    label: { en: Web URL, nl: Web-URL }
    valueType: string
    icon: Globe
    validation:
      pattern: "^https?://"
      maxLength: 2000
    render:
      display: TextDisplay
      input: Input

  mailtoLink:
    label: { en: Email link, nl: E-maillink }
    valueType: string
    icon: Mail
    validation:
      pattern: "^mailto:[^\\s@]+@[^\\s@]+\\.[^\\s@]+(?:\\?.*)?$"
      maxLength: 2000
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: pii
      category: contactgegevens

  phoneLink:
    label: { en: Phone link, nl: Telefoonlink }
    valueType: string
    icon: PhoneCall
    validation:
      pattern: "^tel:\\+?[0-9][0-9\\s\\-().]{5,30}$"
      maxLength: 200
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: pii
      category: contactgegevens

  smsLink:
    label: { en: SMS link, nl: SMS-link }
    valueType: string
    icon: MessageCircle
    validation:
      pattern: "^sms:\\+?[0-9][0-9\\s\\-().]{5,30}(?:\\?.*)?$"
      maxLength: 500
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: pii
      category: contactgegevens

  whatsappLink:
    label: { en: WhatsApp link, nl: WhatsApp-link }
    valueType: string
    icon: MessageCircle
    validation:
      pattern: "^(?:https://wa\\.me/\\+?[0-9]{7,15}(?:\\?.*)?|https://api\\.whatsapp\\.com/send\\?phone=\\+?[0-9]{7,15}(?:&.*)?|whatsapp://send\\?phone=\\+?[0-9]{7,15}(?:&.*)?)$"
      maxLength: 2000
    render:
      display: TextDisplay
      input: Input
    classification:
      sensitivity: pii
      category: contactgegevens

  deeplink:
    label: { en: Deeplink, nl: Deeplink }
    valueType: string
    icon: Link
    validation:
      pattern: "^(?:https?|mailto|tel|sms|whatsapp|openshapeforge|geo|maps):"
      maxLength: 2000
    render:
      display: TextDisplay
      input: Input

  conversationId:
    label: { en: Conversation ID, nl: Conversatie-ID }
    valueType: string
    icon: MessageSquare
    lookup:
      provider: messaging.conversations
      remoteUrl: /api/runtime/lookups?semanticType=conversationId
      searchParam: search
    render:
      display: TextDisplay
      input: Input

  identityAssuranceLevel:
    label: { en: Identity assurance level, nl: Identificatieniveau }
    pluralLabel: { en: Identity assurance levels, nl: Identificatieniveaus }
    valueType: string
    icon: ShieldCheck
    render:
      display: TextDisplay
      input: ReferenceSelect
    props:
      clearable: false
    options:
      type: static
      items:
        - { value: none, label: { en: None, nl: Geen } }
        - { value: contact, label: { en: Contact, nl: Contact } }
        - { value: address, label: { en: Address, nl: Adres } }
        - { value: iban, label: { en: IBAN, nl: IBAN } }
        - { value: verified_identity, label: { en: Verified identity, nl: Geverifieerde identiteit } }
        - { value: manual, label: { en: Manually confirmed, nl: Handmatig bevestigd } }

  workflowDefinitionId:
    label: { en: Workflow definition ID, nl: Workflow-definitie-ID }
    pluralLabel: { en: Workflow definition IDs, nl: Workflow-definitie-IDs }
    valueType: string
    icon: Workflow
    render:
      display: WorkflowDefinitionReferenceDisplay
      input: Input
    # Generic workflow-definition fields use a plain input; case authoring
    # should use caseTemplateId / caseStepTemplateId instead of category-filtered
    # workflow-definition pickers.

  postalCode:
    label: { en: Postal code, nl: Postcode }
    valueType: string
    icon: MapPin
    validation:
      pattern: "^[0-9]{4}\\s?[A-Z]{2}$"
    render:
      display: TextDisplay
      input: Input

  currency:
    label: { en: Currency amount, nl: Geldbedrag }
    valueType: number
    icon: Banknote
    render:
      display: MoneyInput
      input: MoneyInput
    props:
      currency: EUR

  condition:
    valueType: object
    label: { en: Condition, nl: Conditie }
    icon: GitBranch
    render:
      display: TextDisplay
      input: ConditionBuilder
    props:
      mode: free

  variableTemplate:
    valueType: string
    label: { en: Variable template, nl: Variabele-template }
    icon: Braces
    render:
      display: TextDisplay
      input: VariableTemplateInput

  fieldDefinition:
    valueType: object
    label: { en: Field definition, nl: Velddefinitie }
    pluralLabel: { en: Field definitions, nl: Velddefinities }
    icon: ListChecks
    render:
      display: FieldDefinitionView
      input: JsonFieldEditor

  # ----------------------------------------------------------------
  # Entity-ID semantic types (kind: entityId)
  # Hand-authored. Each entry is the canonical pointer to one core
  # entity and drives the workflow designer's option-picker via
  # /api/workflow/designer/core-entity-options. To add a new entity:
  # (1) drop a YAML in entities/ with `semanticType: <X>Id` on its
  # id field, (2) add a matching entry below.
  # ----------------------------------------------------------------

  accountId:
    kind: entityId
    label: { en: "Account ID", nl: "Account ID" }
    pluralLabel: { en: "Account IDs", nl: "Account IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: account
    listUrl: /api/workflow/designer/core-entity-options?entity=account
    displayTemplate: "{{username}}"
    filterField: username
  addressAssignmentId:
    kind: entityId
    label: { en: "Address Assignment ID", nl: "Adrestoewijzing ID" }
    pluralLabel: { en: "Address Assignment IDs", nl: "Adrestoewijzing IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: address-assignment
    listUrl: /api/workflow/designer/core-entity-options?entity=address-assignment
    displayTemplate: "{{role}}"
    filterField: role
  addressId:
    kind: entityId
    label: { en: "Address ID", nl: "Adres ID" }
    pluralLabel: { en: "Address IDs", nl: "Adres IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: address
    listUrl: /api/workflow/designer/core-entity-options?entity=address
    displayTemplate: "{{street}} {{houseNumber}}"
    filterField: street
  agreementId:
    kind: entityId
    label: { en: "Agreement ID", nl: "Overeenkomst ID" }
    pluralLabel: { en: "Agreement IDs", nl: "Overeenkomst IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: agreement
    listUrl: /api/workflow/designer/core-entity-options?entity=agreement
    displayTemplate: "{{code}}"
    filterField: code
  appointmentId:
    kind: entityId
    label: { en: "Appointment ID", nl: "Afspraak ID" }
    pluralLabel: { en: "Appointment IDs", nl: "Afspraak IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: appointment
    listUrl: /api/workflow/designer/core-entity-options?entity=appointment
    displayTemplate: "{{subject}}"
    filterField: subject
  assignmentId:
    kind: entityId
    label: { en: "Assignment ID", nl: "Toewijzing ID" }
    pluralLabel: { en: "Assignment IDs", nl: "Toewijzing IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: assignment
    listUrl: /api/workflow/designer/core-entity-options?entity=assignment
    displayTemplate: "{{reason}}"
    filterField: reason
  attachmentId:
    kind: entityId
    label: { en: "Attachment ID", nl: "Bijlage ID" }
    pluralLabel: { en: "Attachment IDs", nl: "Bijlage IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: attachment
    listUrl: /api/workflow/designer/core-entity-options?entity=attachment
    displayTemplate: "{{fileName}}"
    filterField: fileName
  caseObjectLinkId:
    kind: entityId
    label: { en: "Case Object Link ID", nl: "Zaakobjectkoppeling ID" }
    pluralLabel: { en: "Case Object Link IDs", nl: "Zaakobjectkoppeling IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-object-link
    listUrl: /api/workflow/designer/core-entity-options?entity=case-object-link
    displayTemplate: "{{objectLabel}}"
    filterField: objectLabel
  casePartyId:
    kind: entityId
    label: { en: "Case Party ID", nl: "Zaakbetrokkene ID" }
    pluralLabel: { en: "Case Party IDs", nl: "Zaakbetrokkene IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-party
    listUrl: /api/workflow/designer/core-entity-options?entity=case-party
    displayTemplate: "{{displayName}}"
    filterField: displayName
  caseStatusEventId:
    kind: entityId
    label: { en: "Case Status Event ID", nl: "Zaakstatusgebeurtenis ID" }
    pluralLabel: { en: "Case Status Event IDs", nl: "Zaakstatusgebeurtenis IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-status-event
    listUrl: /api/workflow/designer/core-entity-options?entity=case-status-event
    displayTemplate: "{{actorType}}"
    filterField: actorId
  caseTypeId:
    kind: entityId
    label: { en: "Case Type ID", nl: "Zaaktype ID" }
    pluralLabel: { en: "Case Type IDs", nl: "Zaaktype IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-type
    listUrl: /api/workflow/designer/core-entity-options?entity=case-type
    displayTemplate: "{{name}}"
    filterField: name
  caseId:
    kind: entityId
    label: { en: "Case ID", nl: "Zaak ID" }
    pluralLabel: { en: "Case IDs", nl: "Zaak IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case
    listUrl: /api/workflow/designer/core-entity-options?entity=case
    displayTemplate: "{{title}}"
    filterField: title
  commentId:
    kind: entityId
    label: { en: "Comment ID", nl: "Opmerking ID" }
    pluralLabel: { en: "Comment IDs", nl: "Opmerking IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: comment
    listUrl: /api/workflow/designer/core-entity-options?entity=comment
    displayTemplate: "{{body}}"
    filterField: body
  communicationPreferenceId:
    kind: entityId
    label: { en: "Communication Preference ID", nl: "Communicatievoorkeur ID" }
    pluralLabel: { en: "Communication Preference IDs", nl: "Communicatievoorkeur IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: communication-preference
    listUrl: /api/workflow/designer/core-entity-options?entity=communication-preference
    displayTemplate: "{{type}}"
    filterField: type
  contactDetailId:
    kind: entityId
    label: { en: "Contact Detail ID", nl: "Contactgegeven ID" }
    pluralLabel: { en: "Contact Detail IDs", nl: "Contactgegeven IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: contact-detail
    listUrl: /api/workflow/designer/core-entity-options?entity=contact-detail
    displayTemplate: "{{value}}"
    filterField: value
  contactMomentId:
    kind: entityId
    label: { en: "Contact Moment ID", nl: "Contactmoment ID" }
    pluralLabel: { en: "Contact Moment IDs", nl: "Contactmoment IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: contact-moment
    listUrl: /api/workflow/designer/core-entity-options?entity=contact-moment
    displayTemplate: "{{subject}}"
    filterField: subject
  documentTypeId:
    kind: entityId
    label: { en: "Document Type ID", nl: "Documenttype ID" }
    pluralLabel: { en: "Document Type IDs", nl: "Documenttype IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: document-type
    listUrl: /api/workflow/designer/core-entity-options?entity=document-type
    displayTemplate: "{{name}}"
    filterField: name
  documentVersionId:
    kind: entityId
    label: { en: "Document Version ID", nl: "Documentversie ID" }
    pluralLabel: { en: "Document Version IDs", nl: "Documentversie IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: document-version
    listUrl: /api/workflow/designer/core-entity-options?entity=document-version
    displayTemplate: "{{versionLabel}}"
    filterField: versionLabel
  documentId:
    kind: entityId
    label: { en: "Document ID", nl: "Document ID" }
    pluralLabel: { en: "Document IDs", nl: "Document IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: document
    listUrl: /api/workflow/designer/core-entity-options?entity=document
    displayTemplate: "{{title}}"
    filterField: title
  caseFileTypeId:
    kind: entityId
    label: { en: "Case file type ID", nl: "Dossiertype ID" }
    pluralLabel: { en: "Case file type IDs", nl: "Dossiertype IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-file-type
    listUrl: /api/workflow/designer/core-entity-options?entity=case-file-type
    displayTemplate: "{{name}}"
    filterField: name
  caseFileId:
    kind: entityId
    label: { en: "Case file ID", nl: "Dossier ID" }
    pluralLabel: { en: "Case file IDs", nl: "Dossier IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-file
    listUrl: /api/workflow/designer/core-entity-options?entity=case-file
    displayTemplate: "{{title}}"
    filterField: title
  labelRuleId:
    kind: entityId
    label: { en: "Label Rule ID", nl: "Labelregel ID" }
    pluralLabel: { en: "Label Rule IDs", nl: "Labelregel IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: label-rule
    listUrl: /api/workflow/designer/core-entity-options?entity=label-rule
    displayTemplate: "{{name}}"
    filterField: name
  legalEntityId:
    kind: entityId
    label: { en: "Legal Entity ID", nl: "Rechtspersoon ID" }
    pluralLabel: { en: "Legal Entity IDs", nl: "Rechtspersoon IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: legal-entity
    listUrl: /api/workflow/designer/core-entity-options?entity=legal-entity
    displayTemplate: "{{name}}"
    filterField: name
  milestoneId:
    kind: entityId
    label: { en: "Milestone ID", nl: "Mijlpaal ID" }
    pluralLabel: { en: "Milestone IDs", nl: "Mijlpaal IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: milestone
    listUrl: /api/workflow/designer/core-entity-options?entity=milestone
    displayTemplate: "{{name}}"
    filterField: name
  naturalPersonId:
    kind: entityId
    label: { en: "Natural Person ID", nl: "Natuurlijk Persoon ID" }
    pluralLabel: { en: "Natural Person IDs", nl: "Natuurlijk Persoon IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: natural-person
    listUrl: /api/workflow/designer/core-entity-options?entity=natural-person
    displayTemplate: "{{firstName}} {{lastName}}"
    filterField: lastName
  paymentDetailId:
    kind: entityId
    label: { en: "Payment Detail ID", nl: "Betaalgegevens ID" }
    pluralLabel: { en: "Payment Detail IDs", nl: "Betaalgegevens IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: payment-detail
    listUrl: /api/workflow/designer/core-entity-options?entity=payment-detail
    displayTemplate: "{{iban}}"
    filterField: iban
  preferenceId:
    kind: entityId
    label: { en: "Preference ID", nl: "Voorkeur ID" }
    pluralLabel: { en: "Preference IDs", nl: "Voorkeur IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: preference
    listUrl: /api/workflow/designer/core-entity-options?entity=preference
    displayTemplate: "{{key}}"
    filterField: key
  processStepId:
    kind: entityId
    label: { en: "Process Step ID", nl: "Processtap ID" }
    pluralLabel: { en: "Process Step IDs", nl: "Processtap IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: process-step
    listUrl: /api/workflow/designer/core-entity-options?entity=process-step
    displayTemplate: "{{name}}"
    filterField: name
  caseStepId:
    kind: entityId
    label: { en: "Case Step ID", nl: "Zaakstap ID" }
    pluralLabel: { en: "Case Step IDs", nl: "Zaakstap IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: process-step
    listUrl: /api/workflow/designer/core-entity-options?entity=process-step
    displayTemplate: "{{name}}"
    filterField: name
  caseStepTemplateId:
    kind: entityId
    label: { en: "Case Step Template ID", nl: "Zaakstapsjabloon ID" }
    pluralLabel: { en: "Case Step Template IDs", nl: "Zaakstapsjabloon IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-step-template
    listUrl: /api/workflow/designer/core-entity-options?entity=case-step-template
    displayTemplate: "{{name}}"
    filterField: name
  caseTemplateId:
    kind: entityId
    label: { en: "Case Template ID", nl: "Zaaksjabloon ID" }
    pluralLabel: { en: "Case Template IDs", nl: "Zaaksjabloon IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-template
    listUrl: /api/workflow/designer/core-entity-options?entity=case-template
    displayTemplate: "{{name}}"
    filterField: name
  processTemplateId:
    kind: entityId
    label: { en: "Process Template ID", nl: "Processjabloon ID" }
    pluralLabel: { en: "Process Template IDs", nl: "Processjabloon IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-template
    listUrl: /api/workflow/designer/core-entity-options?entity=case-template
    displayTemplate: "{{name}}"
    filterField: name
  processTemplateStepId:
    kind: entityId
    label: { en: "Process Template Step ID", nl: "Processjabloonstap ID" }
    pluralLabel: { en: "Process Template Step IDs", nl: "Processjabloonstap IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: case-step-template
    listUrl: /api/workflow/designer/core-entity-options?entity=case-step-template
    displayTemplate: "{{name}}"
    filterField: name
  processId:
    kind: entityId
    label: { en: "Process ID", nl: "Proces ID" }
    pluralLabel: { en: "Process IDs", nl: "Proces IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: process
    listUrl: /api/workflow/designer/core-entity-options?entity=process
    displayTemplate: "{{name}}"
    filterField: name
  qualityMeasurementRequestId:
    kind: entityId
    label: { en: "Quality Measurement Request ID", nl: "Kwaliteitsmetingsverzoek ID" }
    pluralLabel: { en: "Quality Measurement Request IDs", nl: "Kwaliteitsmetingsverzoek IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: quality-measurement-request
    listUrl: /api/workflow/designer/core-entity-options?entity=quality-measurement-request
    displayTemplate: "{{title}}"
    filterField: title
  qualityMeasurementResultId:
    kind: entityId
    label: { en: "Quality Measurement Result ID", nl: "Kwaliteitsmetingsresultaat ID" }
    pluralLabel: { en: "Quality Measurement Result IDs", nl: "Kwaliteitsmetingsresultaat IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: quality-measurement-result
    listUrl: /api/workflow/designer/core-entity-options?entity=quality-measurement-result
    displayTemplate: "{{code}}"
    filterField: code
  relationAssociationId:
    kind: entityId
    label: { en: "Relation Association ID", nl: "Relatiekoppeling ID" }
    pluralLabel: { en: "Relation Association IDs", nl: "Relatiekoppeling IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: relation-association
    listUrl: /api/workflow/designer/core-entity-options?entity=relation-association
    displayTemplate: "{{associationKind}}"
    filterField: associationKind
  relationGroupId:
    kind: entityId
    label: { en: "Relation Group ID", nl: "Relatiegroep ID" }
    pluralLabel: { en: "Relation Group IDs", nl: "Relatiegroep IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: relation-group
    listUrl: /api/workflow/designer/core-entity-options?entity=relation-group
    displayTemplate: "{{name}}"
    filterField: name
  relationRoleId:
    kind: entityId
    label: { en: "Relation Role ID", nl: "Relatierol ID" }
    pluralLabel: { en: "Relation Role IDs", nl: "Relatierol IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: relation-role
    listUrl: /api/workflow/designer/core-entity-options?entity=relation-role
    displayTemplate: "{{type}}"
    filterField: type
  relationId:
    kind: entityId
    label: { en: "Relation ID", nl: "Relatie ID" }
    pluralLabel: { en: "Relation IDs", nl: "Relatie IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: relation
    listUrl: /api/workflow/designer/core-entity-options?entity=relation
    displayTemplate: "{{displayName}}"
    filterField: displayName
  researchAssignmentId:
    kind: entityId
    label: { en: "Research Assignment ID", nl: "Onderzoeksopdracht ID" }
    pluralLabel: { en: "Research Assignment IDs", nl: "Onderzoeksopdracht IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: research-assignment
    listUrl: /api/workflow/designer/core-entity-options?entity=research-assignment
    displayTemplate: "{{title}}"
    filterField: title
  researchResultId:
    kind: entityId
    label: { en: "Research Result ID", nl: "Onderzoeksresultaat ID" }
    pluralLabel: { en: "Research Result IDs", nl: "Onderzoeksresultaat IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: research-result
    listUrl: /api/workflow/designer/core-entity-options?entity=research-result
    displayTemplate: "{{code}}"
    filterField: code
  slaPolicyId:
    kind: entityId
    label: { en: "SLA Policy ID", nl: "SLA-beleid ID" }
    pluralLabel: { en: "SLA Policy IDs", nl: "SLA-beleid IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: sla-policy
    listUrl: /api/workflow/designer/core-entity-options?entity=sla-policy
    displayTemplate: "{{name}}"
    filterField: name
  supportIssueId:
    kind: entityId
    label: { en: "Support issue ID", nl: "Supportverzoek ID" }
    pluralLabel: { en: "Support issue IDs", nl: "Supportverzoek IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: support-issue
    listUrl: /api/workflow/designer/core-entity-options?entity=support-issue
    displayTemplate: "{{subject}}"
    filterField: subject
  surveyAnswerId:
    kind: entityId
    label: { en: "Survey Answer ID", nl: "Vragenlijstantwoord ID" }
    pluralLabel: { en: "Survey Answer IDs", nl: "Vragenlijstantwoord IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: survey-answer
    listUrl: /api/workflow/designer/core-entity-options?entity=survey-answer
    displayTemplate: "{{answer}}"
    filterField: answer
  surveyQuestionId:
    kind: entityId
    label: { en: "Survey Question ID", nl: "Vragenlijstregel ID" }
    pluralLabel: { en: "Survey Question IDs", nl: "Vragenlijstregel IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: survey-question
    listUrl: /api/workflow/designer/core-entity-options?entity=survey-question
    displayTemplate: "{{description}}"
    filterField: description
  surveyId:
    kind: entityId
    label: { en: "Survey ID", nl: "Vragenlijst ID" }
    pluralLabel: { en: "Survey IDs", nl: "Vragenlijst IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: survey
    listUrl: /api/workflow/designer/core-entity-options?entity=survey
    displayTemplate: "{{title}}"
    filterField: title
  taskId:
    kind: entityId
    label: { en: "Task ID", nl: "Taak ID" }
    pluralLabel: { en: "Task IDs", nl: "Taak IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: task
    listUrl: /api/workflow/designer/core-entity-options?entity=task
    displayTemplate: "{{title}}"
    filterField: title
  templateVariantId:
    kind: entityId
    label: { en: "Template Variant ID", nl: "Templatevariant ID" }
    pluralLabel: { en: "Template Variant IDs", nl: "Templatevariant IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: template-variant
    listUrl: /api/workflow/designer/core-entity-options?entity=template-variant
    displayTemplate: "{{channel}}"
    filterField: channel
  templateId:
    kind: entityId
    label: { en: "Template ID", nl: "Template ID" }
    pluralLabel: { en: "Template IDs", nl: "Template IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: template
    listUrl: /api/workflow/designer/core-entity-options?entity=template
    displayTemplate: "{{slug}}"
    filterField: slug
  tenantSettingId:
    kind: entityId
    label: { en: "Tenant Setting ID", nl: "Tenantinstelling ID" }
    pluralLabel: { en: "Tenant Setting IDs", nl: "Tenantinstelling IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: tenant-setting
    listUrl: /api/workflow/designer/core-entity-options?entity=tenant-setting
    displayTemplate: "{{key}}"
    filterField: key
  tenantId:
    kind: entityId
    label: { en: "Tenant ID", nl: "Tenant ID" }
    pluralLabel: { en: "Tenant IDs", nl: "Tenant IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: tenant
    listUrl: /api/workflow/designer/core-entity-options?entity=tenant
    displayTemplate: "{{slug}}"
    filterField: slug
  workQueueId:
    kind: entityId
    label: { en: "Work Queue ID", nl: "Werkqueue ID" }
    pluralLabel: { en: "Work Queue IDs", nl: "Werkqueue IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: work-queue
    listUrl: /api/workflow/designer/core-entity-options?entity=work-queue
    displayTemplate: "{{name}}"
    filterField: name
  # Auto-added entityId entries for the new ERP entities (phase 0–10)
  costDimensionId:
    kind: entityId
    label: { en: "Cost Dimension ID", nl: "Cost dimension ID" }
    pluralLabel: { en: "Cost Dimension IDs", nl: "Cost dimension IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: cost-dimension
    listUrl: /api/workflow/designer/core-entity-options?entity=cost-dimension
    displayTemplate: "{{code}} {{name}}"

  vacancyId:
    kind: entityId
    label: { en: "Vacancy ID", nl: "Vacancy ID" }
    pluralLabel: { en: "Vacancy IDs", nl: "Vacancy IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: vacancy
    listUrl: /api/workflow/designer/core-entity-options?entity=vacancy
    displayTemplate: "{{code}} {{title}}"

  projectTaskId:
    kind: entityId
    label: { en: "Project Task ID", nl: "Project task ID" }
    pluralLabel: { en: "Project Task IDs", nl: "Project task IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-task
    listUrl: /api/workflow/designer/core-entity-options?entity=project-task
    displayTemplate: "{{code}} {{name}}"

  buildingId:
    kind: entityId
    label: { en: "Building ID", nl: "Building ID" }
    pluralLabel: { en: "Building IDs", nl: "Building IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: building
    listUrl: /api/workflow/designer/core-entity-options?entity=building
    displayTemplate: "{{code}} {{name}}"

  directDebitMandateId:
    kind: entityId
    label: { en: "Direct Debit Mandate ID", nl: "Direct debit mandate ID" }
    pluralLabel: { en: "Direct Debit Mandate IDs", nl: "Direct debit mandate IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: direct-debit-mandate
    listUrl: /api/workflow/designer/core-entity-options?entity=direct-debit-mandate
    displayTemplate: "{{mandateReference}}"

  invoiceSequenceId:
    kind: entityId
    label: { en: "Invoice Sequence ID", nl: "Invoice sequence ID" }
    pluralLabel: { en: "Invoice Sequence IDs", nl: "Invoice sequence IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: invoice-sequence
    listUrl: /api/workflow/designer/core-entity-options?entity=invoice-sequence
    displayTemplate: "{{kind}} {{fiscalYearCode}}: {{lastNumber}}"

  projectId:
    kind: entityId
    label: { en: "Project ID", nl: "Project ID" }
    pluralLabel: { en: "Project IDs", nl: "Project IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project
    listUrl: /api/workflow/designer/core-entity-options?entity=project
    displayTemplate: "{{code}} {{name}}"

  bankReconciliationId:
    kind: entityId
    label: { en: "Bank Reconciliation ID", nl: "Bank reconciliation ID" }
    pluralLabel: { en: "Bank Reconciliation IDs", nl: "Bank reconciliation IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: bank-reconciliation
    listUrl: /api/workflow/designer/core-entity-options?entity=bank-reconciliation
    displayTemplate: "{{matchedAt}}"

  employeeBudgetId:
    kind: entityId
    label: { en: "Employee Budget ID", nl: "Employee budget ID" }
    pluralLabel: { en: "Employee Budget IDs", nl: "Employee budget IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: employee-budget
    listUrl: /api/workflow/designer/core-entity-options?entity=employee-budget
    displayTemplate: "{{code}} {{name}}"

  projectPhaseDecisionId:
    kind: entityId
    label: { en: "Project Phase Decision ID", nl: "Project phase decision ID" }
    pluralLabel: { en: "Project Phase Decision IDs", nl: "Project phase decision IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-phase-decision
    listUrl: /api/workflow/designer/core-entity-options?entity=project-phase-decision
    displayTemplate: "{{outcome}}"

  paymentMethodId:
    kind: entityId
    label: { en: "Payment Method ID", nl: "Payment method ID" }
    pluralLabel: { en: "Payment Method IDs", nl: "Payment method IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: payment-method
    listUrl: /api/workflow/designer/core-entity-options?entity=payment-method
    displayTemplate: "{{code}}"

  employeeId:
    kind: entityId
    label: { en: "Employee ID", nl: "Employee ID" }
    pluralLabel: { en: "Employee IDs", nl: "Employee IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: employee
    listUrl: /api/workflow/designer/core-entity-options?entity=employee
    displayTemplate: "{{employeeNumber}}"

  buildingPartId:
    kind: entityId
    label: { en: "Building Part ID", nl: "Building part ID" }
    pluralLabel: { en: "Building Part IDs", nl: "Building part IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: building-part
    listUrl: /api/workflow/designer/core-entity-options?entity=building-part
    displayTemplate: "{{code}} {{name}}"

  assetGroupId:
    kind: entityId
    label: { en: "Asset Group ID", nl: "Asset group ID" }
    pluralLabel: { en: "Asset Group IDs", nl: "Asset group IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: asset-group
    listUrl: /api/workflow/designer/core-entity-options?entity=asset-group
    displayTemplate: "{{code}} {{name}}"

  spaceId:
    kind: entityId
    label: { en: "Space ID", nl: "Space ID" }
    pluralLabel: { en: "Space IDs", nl: "Space IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: space
    listUrl: /api/workflow/designer/core-entity-options?entity=space
    displayTemplate: "{{code}} {{name}}"

  orgUnitId:
    kind: entityId
    label: { en: "Organizational Unit ID", nl: "Org unit ID" }
    pluralLabel: { en: "Organizational Unit IDs", nl: "Org unit IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: org-unit
    listUrl: /api/workflow/designer/core-entity-options?entity=org-unit
    displayTemplate: "{{code}} {{name}}"

  standardPriceId:
    kind: entityId
    label: { en: "Standard Price ID", nl: "Standard price ID" }
    pluralLabel: { en: "Standard Price IDs", nl: "Standard price IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: standard-price
    listUrl: /api/workflow/designer/core-entity-options?entity=standard-price
    displayTemplate: "{{code}} {{name}}"

  paymentPlanInstalmentId:
    kind: entityId
    label: { en: "Payment Plan Instalment ID", nl: "Payment plan instalment ID" }
    pluralLabel: { en: "Payment Plan Instalment IDs", nl: "Payment plan instalment IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: payment-plan-instalment
    listUrl: /api/workflow/designer/core-entity-options?entity=payment-plan-instalment
    displayTemplate: "#{{instalmentNumber}}: {{amount}}"

  ledgerPostingLineId:
    kind: entityId
    label: { en: "Ledger Posting Line ID", nl: "Ledger posting line ID" }
    pluralLabel: { en: "Ledger Posting Line IDs", nl: "Ledger posting line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: ledger-posting-line
    listUrl: /api/workflow/designer/core-entity-options?entity=ledger-posting-line
    displayTemplate: "{{lineNumber}}: {{amount}}"

  periodBalanceId:
    kind: entityId
    label: { en: "Period Balance ID", nl: "Period balance ID" }
    pluralLabel: { en: "Period Balance IDs", nl: "Period balance IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: period-balance
    listUrl: /api/workflow/designer/core-entity-options?entity=period-balance
    displayTemplate: "{{ledgerAccount.code}}"

  alertId:
    kind: entityId
    label: { en: "Alert ID", nl: "Alert ID" }
    pluralLabel: { en: "Alert IDs", nl: "Alert IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: alert
    listUrl: /api/workflow/designer/core-entity-options?entity=alert
    displayTemplate: "{{code}} {{title}}"

  areaId:
    kind: entityId
    label: { en: "Area ID", nl: "Area ID" }
    pluralLabel: { en: "Area IDs", nl: "Area IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: area
    listUrl: /api/workflow/designer/core-entity-options?entity=area
    displayTemplate: "{{value}} {{unitId}}"

  invoiceVatBucketId:
    kind: entityId
    label: { en: "Invoice VAT Bucket ID", nl: "Invoice VAT bucket ID" }
    pluralLabel: { en: "Invoice VAT Bucket IDs", nl: "Invoice VAT bucket IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: invoice-vat-bucket
    listUrl: /api/workflow/designer/core-entity-options?entity=invoice-vat-bucket
    displayTemplate: "{{vatRate.code}}"

  creditorId:
    kind: entityId
    label: { en: "Creditor ID", nl: "Creditor ID" }
    pluralLabel: { en: "Creditor IDs", nl: "Creditor IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: creditor
    listUrl: /api/workflow/designer/core-entity-options?entity=creditor
    displayTemplate: "{{creditorNumber}}"

  budgetLineId:
    kind: entityId
    label: { en: "Budget Line ID", nl: "Budget line ID" }
    pluralLabel: { en: "Budget Line IDs", nl: "Budget line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: budget-line
    listUrl: /api/workflow/designer/core-entity-options?entity=budget-line
    displayTemplate: "{{description}}"

  processRoleId:
    kind: entityId
    label: { en: "Process Role ID", nl: "Process role ID" }
    pluralLabel: { en: "Process Role IDs", nl: "Process role IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: process-role
    listUrl: /api/workflow/designer/core-entity-options?entity=process-role
    displayTemplate: "{{roleType}}"

  positionId:
    kind: entityId
    label: { en: "Position ID", nl: "Position ID" }
    pluralLabel: { en: "Position IDs", nl: "Position IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: position
    listUrl: /api/workflow/designer/core-entity-options?entity=position
    displayTemplate: "{{code}} {{name}}"

  projectSpendingId:
    kind: entityId
    label: { en: "Project Spending ID", nl: "Project spending ID" }
    pluralLabel: { en: "Project Spending IDs", nl: "Project spending IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-spending
    listUrl: /api/workflow/designer/core-entity-options?entity=project-spending
    displayTemplate: "{{description}}: {{amount}}"

  energyPerformanceId:
    kind: entityId
    label: { en: "Energy Performance ID", nl: "Energy performance ID" }
    pluralLabel: { en: "Energy Performance IDs", nl: "Energy performance IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: energy-performance
    listUrl: /api/workflow/designer/core-entity-options?entity=energy-performance
    displayTemplate: "{{code}}"

  bankStatementId:
    kind: entityId
    label: { en: "Bank Statement ID", nl: "Bank statement ID" }
    pluralLabel: { en: "Bank Statement IDs", nl: "Bank statement IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: bank-statement
    listUrl: /api/workflow/designer/core-entity-options?entity=bank-statement
    displayTemplate: "{{accountIban}} #{{statementNumber}}"

  appointmentRequestId:
    kind: entityId
    label: { en: "Appointment Request ID", nl: "Appointment request ID" }
    pluralLabel: { en: "Appointment Request IDs", nl: "Appointment request IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: appointment-request
    listUrl: /api/workflow/designer/core-entity-options?entity=appointment-request
    displayTemplate: "{{code}} {{title}}"

  agreementLinkId:
    kind: entityId
    label: { en: "Agreement Link ID", nl: "Agreement link ID" }
    pluralLabel: { en: "Agreement Link IDs", nl: "Agreement link IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: agreement-link
    listUrl: /api/workflow/designer/core-entity-options?entity=agreement-link
    displayTemplate: "{{linkType}}"

  supplierRoleId:
    kind: entityId
    label: { en: "Supplier Role ID", nl: "Supplier ID" }
    pluralLabel: { en: "Supplier Role IDs", nl: "Supplier IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: supplier-role
    listUrl: /api/workflow/designer/core-entity-options?entity=supplier-role
    displayTemplate: "{{supplierNumber}}"

  monitorLogId:
    kind: entityId
    label: { en: "Monitor Log ID", nl: "Monitor log ID" }
    pluralLabel: { en: "Monitor Log IDs", nl: "Monitor log IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: monitor-log
    listUrl: /api/workflow/designer/core-entity-options?entity=monitor-log
    displayTemplate: "{{code}}"

  employeeRoleId:
    kind: entityId
    label: { en: "Employee Role ID", nl: "Employee role ID" }
    pluralLabel: { en: "Employee Role IDs", nl: "Employee role IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: employee-role
    listUrl: /api/workflow/designer/core-entity-options?entity=employee-role
    displayTemplate: "{{roleKind}}"

  billingScheduleId:
    kind: entityId
    label: { en: "Billing Schedule ID", nl: "Facturatieschema ID" }
    pluralLabel: { en: "Billing Schedule IDs", nl: "Facturatieschema IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: billing-schedule
    listUrl: /api/workflow/designer/core-entity-options?entity=billing-schedule
    displayTemplate: "{{period}}"

  billingRunId:
    kind: entityId
    label: { en: "Billing Run ID", nl: "Facturatie-run ID" }
    pluralLabel: { en: "Billing Run IDs", nl: "Facturatie-run IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: billing-run
    listUrl: /api/workflow/designer/core-entity-options?entity=billing-run
    displayTemplate: "{{mode}} @ {{billUpToDate}}"

  billingRunItemId:
    kind: entityId
    label: { en: "Billing Run Item ID", nl: "Facturatie-run-item ID" }
    pluralLabel: { en: "Billing Run Item IDs", nl: "Facturatie-run-item IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: billing-run-item
    listUrl: /api/workflow/designer/core-entity-options?entity=billing-run-item
    displayTemplate: "{{periodStart}} → {{periodEnd}}"

  invoiceCollectionItemId:
    kind: entityId
    label: { en: "Invoice Collection Item ID", nl: "Invoice collection item ID" }
    pluralLabel: { en: "Invoice Collection Item IDs", nl: "Invoice collection item IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: invoice-collection-item
    listUrl: /api/workflow/designer/core-entity-options?entity=invoice-collection-item
    displayTemplate: "{{amount}}"

  bankStatementLineId:
    kind: entityId
    label: { en: "Bank Statement Line ID", nl: "Bank statement line ID" }
    pluralLabel: { en: "Bank Statement Line IDs", nl: "Bank statement line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: bank-statement-line
    listUrl: /api/workflow/designer/core-entity-options?entity=bank-statement-line
    displayTemplate: "{{counterpartyName}}: {{amount}}"

  projectRoleId:
    kind: entityId
    label: { en: "Project Role ID", nl: "Project role ID" }
    pluralLabel: { en: "Project Role IDs", nl: "Project role IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-role
    listUrl: /api/workflow/designer/core-entity-options?entity=project-role
    displayTemplate: "{{roleType}}"

  agreementComponentCumulativeId:
    kind: entityId
    label: { en: "Agreement Component Cumulative ID", nl: "Component cumulative ID" }
    pluralLabel: { en: "Agreement Component Cumulative IDs", nl: "Component cumulative IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: agreement-component-cumulative
    listUrl: /api/workflow/designer/core-entity-options?entity=agreement-component-cumulative
    displayTemplate: "{{postedAt}}"

  projectBudgetLineId:
    kind: entityId
    label: { en: "Project Budget Line ID", nl: "Project budget line ID" }
    pluralLabel: { en: "Project Budget Line IDs", nl: "Project budget line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-budget-line
    listUrl: /api/workflow/designer/core-entity-options?entity=project-budget-line
    displayTemplate: "{{description}}: {{amount}}"

  paymentOrderId:
    kind: entityId
    label: { en: "Payment Order ID", nl: "Payment order ID" }
    pluralLabel: { en: "Payment Order IDs", nl: "Payment order IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: payment-order
    listUrl: /api/workflow/designer/core-entity-options?entity=payment-order
    displayTemplate: "{{referenceNumber}}"

  quoteLineId:
    kind: entityId
    label: { en: "Quote Line ID", nl: "Quote line ID" }
    pluralLabel: { en: "Quote Line IDs", nl: "Quote line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: quote-line
    listUrl: /api/workflow/designer/core-entity-options?entity=quote-line
    displayTemplate: "{{description}}"

  energyPerformanceMeasurementId:
    kind: entityId
    label: { en: "Energy Performance Measurement ID", nl: "Energy performance measurement ID" }
    pluralLabel: { en: "Energy Performance Measurement IDs", nl: "Energy performance measurement IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: energy-performance-measurement
    listUrl: /api/workflow/designer/core-entity-options?entity=energy-performance-measurement
    displayTemplate: "{{measuredAt}}: {{value}}"

  monitorIntervalId:
    kind: entityId
    label: { en: "Monitor Interval ID", nl: "Monitor interval ID" }
    pluralLabel: { en: "Monitor Interval IDs", nl: "Monitor interval IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: monitor-interval
    listUrl: /api/workflow/designer/core-entity-options?entity=monitor-interval
    displayTemplate: "{{code}}"

  appointmentOptionId:
    kind: entityId
    label: { en: "Appointment Option ID", nl: "Appointment option ID" }
    pluralLabel: { en: "Appointment Option IDs", nl: "Appointment option IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: appointment-option
    listUrl: /api/workflow/designer/core-entity-options?entity=appointment-option
    displayTemplate: "#{{optionNumber}} {{startsAt}}"

  debtorId:
    kind: entityId
    label: { en: "Debtor ID", nl: "Debtor ID" }
    pluralLabel: { en: "Debtor IDs", nl: "Debtor IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: debtor
    listUrl: /api/workflow/designer/core-entity-options?entity=debtor
    displayTemplate: "{{debtorNumber}}"

  projectObjectiveId:
    kind: entityId
    label: { en: "Project Objective ID", nl: "Project objective ID" }
    pluralLabel: { en: "Project Objective IDs", nl: "Project objective IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-objective
    listUrl: /api/workflow/designer/core-entity-options?entity=project-objective
    displayTemplate: "{{code}} {{description}}"

  agreementPartyId:
    kind: entityId
    label: { en: "Agreement Party ID", nl: "Agreement party ID" }
    pluralLabel: { en: "Agreement Party IDs", nl: "Agreement party IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: agreement-party
    listUrl: /api/workflow/designer/core-entity-options?entity=agreement-party
    displayTemplate: "{{partyRole}}"

  monitorReadingId:
    kind: entityId
    label: { en: "Monitor Reading ID", nl: "Monitor reading ID" }
    pluralLabel: { en: "Monitor Reading IDs", nl: "Monitor reading IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: monitor-reading
    listUrl: /api/workflow/designer/core-entity-options?entity=monitor-reading
    displayTemplate: "{{readAt}}: {{value}}"

  adminRegionId:
    kind: entityId
    label: { en: "Administrative Region ID", nl: "Administrative region ID" }
    pluralLabel: { en: "Administrative Region IDs", nl: "Administrative region IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: admin-region
    listUrl: /api/workflow/designer/core-entity-options?entity=admin-region
    displayTemplate: "{{regionLevel}}: {{code}}"

  pointId:
    kind: entityId
    label: { en: "Point ID", nl: "Point ID" }
    pluralLabel: { en: "Point IDs", nl: "Point IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: point
    listUrl: /api/workflow/designer/core-entity-options?entity=point
    displayTemplate: "{{latitude}}, {{longitude}}"

  maintenanceRequestSettlementId:
    kind: entityId
    label: { en: "Maintenance Request Settlement ID", nl: "Maintenance request settlement ID" }
    pluralLabel: { en: "Maintenance Request Settlement IDs", nl: "Maintenance request settlement IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-request-settlement
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-request-settlement
    displayTemplate: "{{description}}: {{amount}}"

  directDebitOrderId:
    kind: entityId
    label: { en: "Direct Debit Order ID", nl: "Direct debit order ID" }
    pluralLabel: { en: "Direct Debit Order IDs", nl: "Direct debit order IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: direct-debit-order
    listUrl: /api/workflow/designer/core-entity-options?entity=direct-debit-order
    displayTemplate: "{{endToEndId}}"

  purchaseOrderId:
    kind: entityId
    label: { en: "Purchase Order ID", nl: "Purchase order ID" }
    pluralLabel: { en: "Purchase Order IDs", nl: "Purchase order IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: purchase-order
    listUrl: /api/workflow/designer/core-entity-options?entity=purchase-order
    displayTemplate: "{{purchaseOrderNumber}}"

  fiscalPeriodId:
    kind: entityId
    label: { en: "Fiscal Period ID", nl: "Fiscal period ID" }
    pluralLabel: { en: "Fiscal Period IDs", nl: "Fiscal period IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: fiscal-period
    listUrl: /api/workflow/designer/core-entity-options?entity=fiscal-period
    displayTemplate: "{{code}}"

  fiscalYearId:
    kind: entityId
    label: { en: "Fiscal Year ID", nl: "Fiscal year ID" }
    pluralLabel: { en: "Fiscal Year IDs", nl: "Fiscal year IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: fiscal-year
    listUrl: /api/workflow/designer/core-entity-options?entity=fiscal-year
    displayTemplate: "{{code}}"

  conditionAssessmentId:
    kind: entityId
    label: { en: "Condition Assessment ID", nl: "Condition assessment ID" }
    pluralLabel: { en: "Condition Assessment IDs", nl: "Condition assessment IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: condition-assessment
    listUrl: /api/workflow/designer/core-entity-options?entity=condition-assessment
    displayTemplate: "{{assessedAt}}"

  invoicePaymentId:
    kind: entityId
    label: { en: "Invoice Payment ID", nl: "Invoice payment ID" }
    pluralLabel: { en: "Invoice Payment IDs", nl: "Invoice payment IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: invoice-payment
    listUrl: /api/workflow/designer/core-entity-options?entity=invoice-payment
    displayTemplate: "{{amount}} @ {{paidAt}}"

  valuationId:
    kind: entityId
    label: { en: "Valuation ID", nl: "Valuation ID" }
    pluralLabel: { en: "Valuation IDs", nl: "Valuation IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: valuation
    listUrl: /api/workflow/designer/core-entity-options?entity=valuation
    displayTemplate: "{{valuationKind}}: {{amount}} @ {{valuedAt}}"

  ledgerPostingId:
    kind: entityId
    label: { en: "Ledger Posting ID", nl: "Ledger posting ID" }
    pluralLabel: { en: "Ledger Posting IDs", nl: "Ledger posting IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: ledger-posting
    listUrl: /api/workflow/designer/core-entity-options?entity=ledger-posting
    displayTemplate: "{{description}}"

  budgetId:
    kind: entityId
    label: { en: "Budget ID", nl: "Budget ID" }
    pluralLabel: { en: "Budget IDs", nl: "Budget IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: budget
    listUrl: /api/workflow/designer/core-entity-options?entity=budget
    displayTemplate: "{{code}} {{name}}"

  vatReturnId:
    kind: entityId
    label: { en: "VAT Return ID", nl: "VAT return ID" }
    pluralLabel: { en: "VAT Return IDs", nl: "VAT return IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: vat-return
    listUrl: /api/workflow/designer/core-entity-options?entity=vat-return
    displayTemplate: "{{referenceNumber}}"

  maintenanceOrderPartyId:
    kind: entityId
    label: { en: "Maintenance Order Party ID", nl: "Maintenance order party ID" }
    pluralLabel: { en: "Maintenance Order Party IDs", nl: "Maintenance order party IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-order-party
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-order-party
    displayTemplate: "{{roleType}}"

  maintenanceTaskId:
    kind: entityId
    label: { en: "Maintenance Task ID", nl: "Maintenance task ID" }
    pluralLabel: { en: "Maintenance Task IDs", nl: "Maintenance task IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-task
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-task
    displayTemplate: "{{description}}"

  assetConditionId:
    kind: entityId
    label: { en: "Asset Condition ID", nl: "Asset condition ID" }
    pluralLabel: { en: "Asset Condition IDs", nl: "Asset condition IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: asset-condition
    listUrl: /api/workflow/designer/core-entity-options?entity=asset-condition
    displayTemplate: "{{assessedAt}}"

  quoteId:
    kind: entityId
    label: { en: "Quote ID", nl: "Quote ID" }
    pluralLabel: { en: "Quote IDs", nl: "Quote IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: quote
    listUrl: /api/workflow/designer/core-entity-options?entity=quote
    displayTemplate: "{{quoteNumber}}"

  timeEntryId:
    kind: entityId
    label: { en: "Time Entry ID", nl: "Time entry ID" }
    pluralLabel: { en: "Time Entry IDs", nl: "Time entry IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: time-entry
    listUrl: /api/workflow/designer/core-entity-options?entity=time-entry
    displayTemplate: "{{workedAt}}: {{hours}}h"

  paymentPlanId:
    kind: entityId
    label: { en: "Payment Plan ID", nl: "Payment plan ID" }
    pluralLabel: { en: "Payment Plan IDs", nl: "Payment plan IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: payment-plan
    listUrl: /api/workflow/designer/core-entity-options?entity=payment-plan
    displayTemplate: "{{code}}"

  assetId:
    kind: entityId
    label: { en: "Asset ID", nl: "Asset ID" }
    pluralLabel: { en: "Asset IDs", nl: "Asset IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: asset
    listUrl: /api/workflow/designer/core-entity-options?entity=asset
    displayTemplate: "{{code}} {{name}}"

  vatRateId:
    kind: entityId
    label: { en: "VAT Rate ID", nl: "VAT rate ID" }
    pluralLabel: { en: "VAT Rate IDs", nl: "VAT rate IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: vat-rate
    listUrl: /api/workflow/designer/core-entity-options?entity=vat-rate
    displayTemplate: "{{code}}"

  priceAdjustmentId:
    kind: entityId
    label: { en: "Price Adjustment ID", nl: "Price adjustment ID" }
    pluralLabel: { en: "Price Adjustment IDs", nl: "Price adjustment IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: price-adjustment
    listUrl: /api/workflow/designer/core-entity-options?entity=price-adjustment
    displayTemplate: "{{code}} {{name}}"

  maintenanceOrderId:
    kind: entityId
    label: { en: "Maintenance Order ID", nl: "Maintenance order ID" }
    pluralLabel: { en: "Maintenance Order IDs", nl: "Maintenance order IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-order
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-order
    displayTemplate: "{{code}} {{title}}"

  maintenanceSpendingId:
    kind: entityId
    label: { en: "Maintenance Spending ID", nl: "Maintenance spending ID" }
    pluralLabel: { en: "Maintenance Spending IDs", nl: "Maintenance spending IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-spending
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-spending
    displayTemplate: "{{description}}: {{amount}}"

  agreementComponentTypeId:
    kind: entityId
    label: { en: "Agreement Component Type ID", nl: "Agreement component type ID" }
    pluralLabel: { en: "Agreement Component Type IDs", nl: "Agreement component type IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: agreement-component-type
    listUrl: /api/workflow/designer/core-entity-options?entity=agreement-component-type
    displayTemplate: "{{code}} {{name}}"

  invoiceLineId:
    kind: entityId
    label: { en: "Invoice Line ID", nl: "Invoice line ID" }
    pluralLabel: { en: "Invoice Line IDs", nl: "Invoice line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: invoice-line
    listUrl: /api/workflow/designer/core-entity-options?entity=invoice-line
    displayTemplate: "{{description}}"

  purchaseOrderLineId:
    kind: entityId
    label: { en: "Purchase Order Line ID", nl: "Purchase order line ID" }
    pluralLabel: { en: "Purchase Order Line IDs", nl: "Purchase order line IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: purchase-order-line
    listUrl: /api/workflow/designer/core-entity-options?entity=purchase-order-line
    displayTemplate: "{{description}}"

  maintenanceTaskCalculationId:
    kind: entityId
    label: { en: "Maintenance Task Calculation ID", nl: "Maintenance task calculation ID" }
    pluralLabel: { en: "Maintenance Task Calculation IDs", nl: "Maintenance task calculation IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-task-calculation
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-task-calculation
    displayTemplate: "{{description}}"

  monitorMeasurementId:
    kind: entityId
    label: { en: "Monitor Measurement ID", nl: "Monitor measurement ID" }
    pluralLabel: { en: "Monitor Measurement IDs", nl: "Monitor measurement IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: monitor-measurement
    listUrl: /api/workflow/designer/core-entity-options?entity=monitor-measurement
    displayTemplate: "{{measuredAt}}"

  ledgerAccountId:
    kind: entityId
    label: { en: "Ledger Account ID", nl: "Ledger account ID" }
    pluralLabel: { en: "Ledger Account IDs", nl: "Ledger account IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: ledger-account
    listUrl: /api/workflow/designer/core-entity-options?entity=ledger-account
    displayTemplate: "{{code}} {{name}}"

  inspectionReportId:
    kind: entityId
    label: { en: "Inspection Report ID", nl: "Inspection report ID" }
    pluralLabel: { en: "Inspection Report IDs", nl: "Inspection report IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: inspection-report
    listUrl: /api/workflow/designer/core-entity-options?entity=inspection-report
    displayTemplate: "{{code}}"

  projectPhaseId:
    kind: entityId
    label: { en: "Project Phase ID", nl: "Project phase ID" }
    pluralLabel: { en: "Project Phase IDs", nl: "Project phase IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-phase
    listUrl: /api/workflow/designer/core-entity-options?entity=project-phase
    displayTemplate: "{{code}} {{name}}"

  invoiceId:
    kind: entityId
    label: { en: "Invoice ID", nl: "Invoice ID" }
    pluralLabel: { en: "Invoice IDs", nl: "Invoice IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: invoice
    listUrl: /api/workflow/designer/core-entity-options?entity=invoice
    displayTemplate: "{{invoiceKind}} {{invoiceNumber}}"

  agreementComponentId:
    kind: entityId
    label: { en: "Agreement Component ID", nl: "Agreement component ID" }
    pluralLabel: { en: "Agreement Component IDs", nl: "Agreement component IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: agreement-component
    listUrl: /api/workflow/designer/core-entity-options?entity=agreement-component
    displayTemplate: "{{description}}"

  encumbranceId:
    kind: entityId
    label: { en: "Encumbrance ID", nl: "Encumbrance ID" }
    pluralLabel: { en: "Encumbrance IDs", nl: "Encumbrance IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: encumbrance
    listUrl: /api/workflow/designer/core-entity-options?entity=encumbrance
    displayTemplate: "{{code}}"

  monitorLogEntryId:
    kind: entityId
    label: { en: "Monitor Log Entry ID", nl: "Monitor log entry ID" }
    pluralLabel: { en: "Monitor Log Entry IDs", nl: "Monitor log entry IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: monitor-log-entry
    listUrl: /api/workflow/designer/core-entity-options?entity=monitor-log-entry
    displayTemplate: "{{loggedAt}}: {{message}}"

  projectOrderId:
    kind: entityId
    label: { en: "Project Order ID", nl: "Project order ID" }
    pluralLabel: { en: "Project Order IDs", nl: "Project order IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: project-order
    listUrl: /api/workflow/designer/core-entity-options?entity=project-order
    displayTemplate: "{{code}}"

  warrantyId:
    kind: entityId
    label: { en: "Warranty ID", nl: "Warranty ID" }
    pluralLabel: { en: "Warranty IDs", nl: "Warranty IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: warranty
    listUrl: /api/workflow/designer/core-entity-options?entity=warranty
    displayTemplate: "{{code}}"

  maintenanceRequestId:
    kind: entityId
    label: { en: "Maintenance Request ID", nl: "Maintenance request ID" }
    pluralLabel: { en: "Maintenance Request IDs", nl: "Maintenance request IDs" }
    valueType: string
    icon: Database
    classification:
      sensitivity: internal
      category: workflow
    entity: maintenance-request
    listUrl: /api/workflow/designer/core-entity-options?entity=maintenance-request
    displayTemplate: "{{code}} {{title}}"
