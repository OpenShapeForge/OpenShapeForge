schemaVersion: 3
kind: coreEntity
module: core
entity: CrudReadOnly
title: CRUD Read Only
description: Test fixture for a common CRUD policy that narrows every transport.
language: en
displayTemplate: "{{name}}"
filterField: name
labels:
  en: CRUD Read Only
domains:
  - relations
authorization:
  roles:
    read:
      - Relaties.All.Read
    create:
      - Relaties.All.ReadWrite
    update:
      - Relaties.All.ReadWrite
    delete:
      - Relaties.All.ReadWrite
fields:
  - key: name
    valueType: string
    required: true
    label:
      en: Name
    persisted:
      column: name
      storageClass: core
operations:
  list:
    name: List CRUD Read Only
    description: List CrudReadOnly records.
    implementation:
      type: entity
      action: list
    effects:
      data: read
      external: none
    reliability:
      idempotency:
        mode: natural
    confirmation:
      mode: none
  get:
    name: Get CRUD Read Only
    description: Get CrudReadOnly records.
    implementation:
      type: entity
      action: get
    effects:
      data: read
      external: none
    reliability:
      idempotency:
        mode: natural
    confirmation:
      mode: none
interfaces:
  graphql: {}
  rest: {}
  mcp: {}
