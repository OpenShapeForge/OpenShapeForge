schemaVersion: 1
kind: coreEntity
module: core
entity: RestCustom
title: Rest Custom
description: "Test fixture — generated REST exposure with a custom base path and a disabled operation. Not part of the shipped slice."
language: en
displayTemplate: "{{name}}"
filterField: name
labels:
  en: Rest Custom
domains:
  - relations
rest:
  enabled: true
  basePath: custom-things
  operations:
    delete: false
authorization:
  roles:
    read:
      - Relaties.All.Read
    create:
      - Relaties.All.ReadWrite
    update:
      - Relaties.All.ReadWrite
    delete:
      - Relaties.All.ReadWrite
fields:
  - key: name
    valueType: string
    required: true
    label:
      en: Name
    persisted:
      column: name
      storageClass: core
