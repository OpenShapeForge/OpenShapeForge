schemaVersion: 1
kind: coreEntity
module: core
entity: RetentionCryptoValid
title: Retention Crypto Valid
description: Test fixture for an explicitly referenced crypto-erasure key.
language: en
displayTemplate: "{{name}}"
filterField: name
domains:
  - relations
authorization:
  roles:
    read:
      - Relaties.All.Read
    create:
      - Relaties.All.ReadWrite
    update:
      - Relaties.All.ReadWrite
    delete:
      - Relaties.All.ReadWrite
retention:
  mode: inline
  duration: P1Y
  startsFrom:
    strategy: createdAt
  legalBasis:
    reference: Legal basis prose, not a key identifier.
  disposition:
    action: cryptoDelete
    cryptoDelete:
      keyReference: retention-subject-key
fields:
  - key: name
    valueType: string
    required: true
    label:
      en: Name
      nl: Naam
    persisted:
      column: name
      storageClass: core
