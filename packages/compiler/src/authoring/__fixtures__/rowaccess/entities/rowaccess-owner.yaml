schemaVersion: 1
kind: coreEntity
module: core
entity: RowAccessOwner
title: Row Access Owner
description: Test fixture — owner axis (belongsTo foreignKey). Not part of the shipped slice.
language: en
displayTemplate: "{{title}}"
filterField: title
labels:
  en: Row Access Owner
  nl: Row Access Owner
domains:
  - relations
authorization:
  roles:
    read:
      - Relaties.All.Read
    create:
      - Relaties.All.ReadWrite
    update:
      - Relaties.All.ReadWrite
    delete:
      - Relaties.All.ReadWrite
  rowAccess:
    enabled: true
    empty: public
    owner:
      column: owner_id
      session: app.current_user_id
fields:
  - key: title
    valueType: string
    required: true
    label:
      en: Title
      nl: Titel
    persisted:
      column: title
      storageClass: core
relationships:
  - key: owner
    kind: belongsTo
    target: RowAccessOwnerTarget
    foreignKey: owner_id
    label:
      en: Owner
      nl: Eigenaar
