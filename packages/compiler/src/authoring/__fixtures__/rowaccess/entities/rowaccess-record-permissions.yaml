schemaVersion: 1
kind: coreEntity
module: core
entity: RowAccessRecordPermissions
title: Row Access Record Permissions
description: Test fixture for action-specific record authorization. Not shipped.
language: en
displayTemplate: "{{title}}"
filterField: title
labels:
  en: Row Access Record Permissions
  nl: Row Access Record Permissions
domains:
  - relations
authorization:
  roles:
    read:
      - Relaties.All.Read
    create:
      - Relaties.All.ReadWrite
    update:
      - Relaties.All.ReadWrite
    delete:
      - Relaties.All.ReadWrite
  rowAccess:
    enabled: true
    empty: restricted
    recordPermissions:
      field: authorization
      empty: public
      createRequires:
        - view
        - edit
fields:
  - key: title
    valueType: string
    required: true
    label:
      en: Title
      nl: Titel
    persisted:
      column: title
      storageClass: core
  - key: authorization
    valueType: object
    required: true
    defaultValue: {}
    label:
      en: Authorization
      nl: Autorisatie
    persisted:
      column: authorization
      storageClass: core
