// Generated by OpenShapeForge Service Compiler
// Route: <%= route %> | Contexts: <%= contexts.join(", ") %>

import {
  render<%= contract.entity.name %>DetailPage,
  type GeneratedDetailPageProps,
} from "<%- sharedPageImportPath %>";
import { detailConfigBases } from "<%- baseImportPath %>";

const routeListRoutes: Record<string, string> = <%- JSON.stringify(listRoutesByContext) %>;
const configs: Record<string, any> = Object.fromEntries(
  Object.entries(detailConfigBases).map(([context, config]) => [
    context,
    {
      ...config,
      listRoute: routeListRoutes[context] ?? config.listRoute,
    },
  ]),
);

export default async function Page(props: GeneratedDetailPageProps) {
  return render<%= contract.entity.name %>DetailPage(configs, <%- JSON.stringify(route) %>, props);
}
