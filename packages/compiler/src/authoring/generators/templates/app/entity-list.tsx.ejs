// Generated by OpenShapeForge Service Compiler
// Route: <%= route %> | Contexts: <%= contexts.join(", ") %>

import { render<%= contract.entity.name %>ListPage } from "<%- sharedPageImportPath %>";
import { listConfigBases } from "<%- baseImportPath %>";

const routeRowLinks: Record<string, string> = <%- JSON.stringify(rowLinksByContext) %>;
const configs: Record<string, any> = Object.fromEntries(
  Object.entries(listConfigBases).map(([context, config]) => [
    context,
    {
      ...config,
      rowLink: routeRowLinks[context] ?? config.rowLink,
    },
  ]),
);
export default async function Page() {
  return render<%= contract.entity.name %>ListPage(configs);
}
